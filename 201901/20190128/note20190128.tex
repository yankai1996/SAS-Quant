
\input{./lib/noteformat}
\usepackage[T1]{fontenc}

\begin{document}


\thispagestyle{fancy}\fancyhead[LO]{$z_{RD}+z_{EMP}$, xUS-one-way-decile-big, Yan Kai}
\fancyhead[RO]{Jan 28, 2019}

\newcommand{\code}{\texttt}
\newcommand*{\Commonpath}{20190128/lagEMP/xUS-one-way-decile-big}


% We only use the data up to 201006.

\code{
	if country in ("CANADA",
"FRANCE",
"GERMANY",
"UNITED KINGDOM",
% "UNITED STATES",
"JAPAN",
"SWEDEN",
"HONG KONG"
"CHINA"
);
}

% We exclude US. 
% We exclude GREECE and AUSTRALIA which are small.
We apply 3/5/10 rules.

We investigate the two-way-spread over $z_{RD/MC}$ and $z_{\Delta EMP/COG}$ at world level, with equal weighted and value weighted. 

% We use the absolute value of $\Delta EMP/COG$, including 0, to do $5\times5$.

% We do one-way-sort using $z$-score:
$$
\begin{aligned}
& z_{RD} = mean(z_1, z_2, z_3) \\
& z_i = z(RD_i/MC) \\
& RD_1 = RD_t \\
& RD_2 = (RD_t + 0.8RD_{t-1} + 0.6RD_{t-2} + 0.4RD_{t-3} + 0.2RD_{t-4})/3 \\
& RD_3 = (RD_t + 0.8RD_{t-1} + 0.6RD_{t-2})/2.4 
& \\
& z_{EMP} = z(|\frac{\Delta EMP}{lag(EMP)}|) \\
& \Delta EMP = EMP_t - EMP_{t-1}
\end{aligned}
$$

Finally, we have
$$
z = z_{RD} + z_{EMP}
$$

% \code{51 <= n <= 1000000}, where \code{n} is the size of the cross section of $z_{RD}$ and $z_{EMP}$.

Filters (all):

\code{if p\_us\_updated>=p\_us\_10;} \\
\code{if ret>-1 and ret<10;} \\
\code{if ret\_us>-1 and ret\_us<10;} \\
% \code{if country in ("AU", "BD", "CH", "CN", "FN", "FR", "GR", "HK", "IN", "IS", "IT", "JP", "KO", "MY","SD", "SG", "SW", "TA", "TK", "UK", "US");}


\small

% 1. country\_ew
% \input{./\Commonpath/country_ew}

% 2. country\_vw
% \input{./\Commonpath/country_vw}

% 3. region\_ew
% \input{./\Commonpath/region_ew}

% 4. region\_vw
% \input{./\Commonpath/region_vw}

5. world\_ew
\input{./\Commonpath/world_ew}

6. world\_vw
\input{./\Commonpath/world_vw}

\end{document}
